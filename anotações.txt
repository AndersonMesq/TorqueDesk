http://localhost:8080/swagger-ui/index.html
http://localhost:8080/v3/api-docs
http://localhost:8080/v3/api-docs/swagger-config

- Modificar UserTenantService para validar criação de User somente sob ADMIN role
- Aplicar JwtClaims a JwtFilter
- Modificar o model e dto de criação dos usuarios e deixar assossiação
- user.getEmail(), em UserPrincipal vai retornar nulo (Problema?)
- "Collection<? extends GrantedAuthority> getAuthorities()" oque isso significa?
- public Collection<? extends GrantedAuthority> getAuthorities(){
          return List.of();
      }
Deve mesmo retornar List.of?
- loadWorkspaces retorna uma lista de DTOs, é um problema?
- instanceof é problematico?
- Diferença entre AllArgsConstructor e @RequiredArgsConstructor
- SecurityContextHolder, pesquisar mais sobre.
- Por que AuthenticationService depender de SecurityUtils é melhor do que depender do SpringSecurity?
- Explicação sobre objetos de dominio.
- Dar uma olhada em como usar @PreAuthorize em conjunto com Beans
-----Prompt IA-------
- Contexto
- Objetivo
- Situação atual
- Restrições
Manter-se na stack, pode sugerir outras ferramentas mas não aplique sem aprovação
- Critérios de qualidade
Clean code e escalabilidade
- Formato esperado
Exemplo abaixo:
Topico X
Codigo:
public class x {
    public int soma(int x, int y){
        return x + y;
    }
}
Explicação:
Metodo retorna x + y

Cuidados/observações:
Não passar string

Outras soluções:
retornar resultado em variavel local
------------------
SUPER_ADMIN,
OWNER,
ATTENDANT,
MECHANIC

@PreAuthorize
Expressão	Função
hasRole("X")	Verifica uma role
hasAnyRole(...)	Verifica várias roles
hasAuthority("X")	Verifica uma permissão
hasAnyAuthority(...)	Verifica várias permissões
isAuthenticated()	Usuário autenticado
isAnonymous()	Usuário não autenticado
and	E lógico
or	OU lógico
!	Negação
#parametro	Acessa parâmetros do método
authentication	Acessa informações do usuário autenticado
@bean.metodo(...)	Executa uma validação personalizada